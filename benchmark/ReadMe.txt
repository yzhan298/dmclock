The purpose of this script is to compare two distribution of dmclock simulators.

0. This script requires python, gnuplot, and awk
1. Run run.sh in sudo mode as the script clears the cache which requires admin permission.
  sudo ./run.sh
  
2. The the output is 'benchmark.pdf'
